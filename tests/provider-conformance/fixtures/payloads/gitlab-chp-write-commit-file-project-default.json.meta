# P3-4 R10 — chp_gitlab_commit_file default-branch probe for orphan bootstrap.
#
# gitlab_version=17.x
# reference_endpoint=GET /projects/:id
#
# Only fetched when the target branch is absent (branch-preflight 404 arm);
# the leaf reads .default_branch to feed the POST /repository/branches `ref=` param.
