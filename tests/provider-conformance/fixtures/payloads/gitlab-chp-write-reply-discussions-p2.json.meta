# P3-4 R6 — chp_gitlab_reply_review_comment discussions walk, page 2 (match).
#
# gitlab_version=17.x
# reference_endpoint=GET /projects/:id/merge_requests/:iid/discussions
#
# The test target COMMENT_ID is 200002; its owning discussion is `d099`.
