Codex review of PR #999 (origin/main...feat/example)

I reviewed the scoped diff (the PR's merge-target range).

Findings:

[P2] src/handler.ts:88 — minor: this helper could reuse the existing
`normalizePath` util instead of re-implementing the trim. Non-blocking.

[P3] tests/handler.test.ts:10 — consider adding an assertion for the empty-input
case. Non-blocking.

Summary: no blocking findings. The change is correct, tests cover the new
behavior, and CI is green. Looks good to merge.
